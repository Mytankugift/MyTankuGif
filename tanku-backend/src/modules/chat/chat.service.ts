/**
 * Chat Service
 * 
 * ⚠️ MÓDULO PREPARADO - NO IMPLEMENTADO
 * 
 * Este servicio está preparado para implementar mensajería directa.
 * Ver README.md para instrucciones de implementación y prompt completo.
 * 
 * NOTA: Solo chat directo (1-a-1), NO chat grupal.
 */

import { prisma } from '../../config/database';

export class ChatService {
  // TODO: Implementar métodos según README.md
  // - createOrGetConversation(userId, participantId)
  // - getConversations(userId)
  // - getConversationById(conversationId, userId)
  // - getMessages(conversationId, userId, pagination)
  // - sendMessage(conversationId, userId, data)
  // - markAsRead(conversationId, userId)
  // - getUnreadCount(userId)
  // - updateConversationLastMessage(conversationId)
  
  // IMPORTANTE: Integrar Socket.IO cuando se envían mensajes
  // Ver README.md para detalles de integración
}

